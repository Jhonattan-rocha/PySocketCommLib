# Dependências obrigatórias
cryptography>=41.0.0

# Dependências opcionais — instale conforme o que for usar:
# pip install PySocketCommLib[postgres]   → suporte a PostgreSQL
# pip install PySocketCommLib[redis]      → cache Redis
# pip install PySocketCommLib[monitoring] → métricas de sistema (CPU/memória/disco)
# pip install PySocketCommLib[all]        → todas as opcionais de uma vez
#
# psycopg2-binary>=2.9.0
# redis>=4.0.0
# psutil>=5.9.0
